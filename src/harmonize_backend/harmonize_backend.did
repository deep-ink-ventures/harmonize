service : (text, principal) -> {
  check_balance : (nat32, text, opt text) -> (nat) query;
  check_balance_native : (nat32) -> (nat) query;
  get_challenge : (text) -> (text) query;
  set_network_config : (nat32, vec text, nat32) -> ();
  set_owner : (principal) -> ();
  sync_logs : () -> ();
  withdraw : (text, opt text, nat) -> ();
  withdraw_native : (text, nat) -> ();
}
